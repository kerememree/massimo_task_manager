<!DOCTYPE html>
<html>
  <head>
    <title>Gorev Yonetim Sistemi</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <script src="https://cdn.tailwindcss.com"></script>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 font-sans">
    <nav class="bg-blue-600 p-4 text-white shadow-lg mb-8">
      <div class="container mx-auto flex justify-between items-center">
        <h1 class="text-xl font-bold italic tracking-tight">Massimo Task Manager</h1>
        <div class="space-x-4">
          <% if user_signed_in? %>
            <span class="text-sm border-r pr-4 border-blue-400">Hoş geldin, <%= current_user.name %></span>
            <%= link_to "Görevlerim", root_path, class: "hover:underline" %>
            <%= link_to "Çıkış Yap", destroy_user_session_path, data: { turbo_method: :delete }, class: "bg-red-500 px-3 py-1 rounded hover:bg-red-600 transition" %>
          <% else %>
            <%= link_to "Giriş Yap", new_user_session_path, class: "hover:underline" %>
            <%= link_to "Kayıt Ol", new_user_registration_path, class: "bg-white text-blue-600 px-3 py-1 rounded hover:bg-gray-100 transition" %>
          <% end %>
        </div>
      </div>
    </nav>

    <main class="container mx-auto px-4 max-w-4xl">
      <% if notice %>
        <div class="bg-green-100 border-l-4 border-green-500 text-green-700 p-4 mb-6 shadow-sm" role="alert">
          <%= notice %>
        </div>
      <% end %>
      <% if alert %>
        <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-6 shadow-sm" role="alert">
          <%= alert %>
        </div>
      <% end %>

      <div class="bg-white p-8 rounded-xl shadow-md border border-gray-200">
        <%= yield %>
      </div>
    </main>
  </body>
</html>